---
layout: base.njk
title: Tidbits
permalink: "/tidbits/"
---

<p><a href="{{ '/' | url }}">&lt;</a></p>
<h1>Small tidbits</h1>
<hr>
<ul class="tidbits-list" style="list-style: none; padding-left: 0;">
  {% for post in collections.tidbits %}
    <li style="margin: 1.25rem 0;">
      <a href="{{ post.url }}">{{ post.data.title or (post.fileSlug | replace('-', ' ')) }}</a>
      <small> — {{ post.date | date("LLL d, yyyy") }}</small>
    </li>
  {% endfor %}
</ul>

